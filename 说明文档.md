# WeTerm — 说明文档

## 项目简介

**WeTerm** 是一款类 MobaXterm 的 SSH/SFTP 客户端，基于 **Tauri v2 + Rust + React + TypeScript** 构建，专为 macOS 设计。提供 SSH 终端、SFTP 文件管理、文本编辑器、拖拽传输、连接历史等功能。

---

## 技术栈

| 层 | 技术 |
|---|---|
| 桌面框架 | Tauri v2 |
| 后端语言 | Rust |
| SSH 库 | `ssh2` (libssh2 绑定) |
| 前端框架 | React 19 |
| 构建工具 | Vite 6 |
| 终端模拟 | xterm.js + xterm-addon-fit |
| 序列化 | serde / serde_json |

---

## 项目结构

```
termius/
├── src/                          # React 前端
│   ├── App.tsx                   # 主布局：TopBar + Sidebar(Remote/Local/History) + MainArea(终端/编辑器)
│   ├── App.css                   # 全部样式（深色主题）
│   ├── types.ts                  # 类型定义
│   ├── components/
│   │   ├── TopBar.tsx            # 顶部栏：连接标签页 + "+" 新建连接弹窗
│   │   ├── FileBrowser.tsx       # 文件浏览器（远程/本地）
│   │   ├── SshTerminal.tsx       # SSH 终端（xterm.js + 50ms 轮询回读）
│   │   ├── FileEditor.tsx        # 远程文件文本编辑器（读取/修改/保存/撤销）
│   │   ├── TransferBar.tsx       # 传输进度条（速度 + ETA + 已完成/总量）
│   │   └── HistoryList.tsx       # 连接历史列表
├── src-tauri/                    # Rust 后端
│   ├── src/
│   │   ├── lib.rs                # Tauri 入口：注册全部命令 + 连接历史持久化
│   │   ├── ssh_manager.rs        # SSH 连接管理（连接/断开/PTY shell/命令执行/SFTP）
│   │   └── sftp_manager.rs       # SFTP 文件操作（列表/下载/上传/删除/创建目录/重命名）
│   ├── Cargo.toml
│   ├── tauri.conf.json           # Tauri 配置（窗口/包名/构建命令）
│   └── icons/                    # 应用图标
├── package.json
├── tsconfig.json
├── vite.config.ts
└── index.html
```

---

## 已实现功能

### 连接管理
- [x] 新建连接弹窗（Host / Port / Username / Auth 类型选择）
- [x] 三种认证方式：Password / SSH Key / SSH Agent
- [x] 多标签页管理（切换 / 关闭）
- [x] 连接历史持久化（保存至 `~/.weterm/connections.json`，最近 20 条）
- [x] 从历史记录一键重连

### SSH 终端
- [x] 持久 PTY Shell 会话（xterm-256color, 80×24）
- [x] 键盘输入即时发送远端 + 远端回显显示
- [x] 50ms 轮询回读（防止输出丢失）
- [x] xterm.js 深色主题配色（GitHub Dark 风格）
- [x] 2000 行回滚缓冲区
- [x] 自定义滚动条样式

### SFTP 文件管理
- [x] 远程文件列表（递归读取目录 + 排序列：目录在前，名称排序）
- [x] 本地文件列表（支持 `~` 展开）
- [x] 双击目录进入 / `..` 返回上级
- [x] 文件大小格式化显示（B/KB/MB/GB）

### 文件编辑器
- [x] 双击远程文件以文本模式打开
- [x] 在线编辑 + 保存回远端
- [x] 撤销修改（恢复到原始内容）
- [x] 文本区使用等宽字体（JetBrains Mono / SF Mono）

### 文件传输
- [x] 拖拽文件从远程面板 → 本地面板（下载到 `~/Desktop`）
- [x] 拖拽文件从本地面板 → 远程面板（上传到当前远程目录）
- [x] 实时传输进度推送（Tauri Event: `transfer-progress`）
- [x] 传输信息栏：文件名、方向、速度、ETA
- [x] 传输完成后 4 秒自动消失

### UI 布局
- [x] 三栏式布局：左侧文件浏览器（Remote / Local / History）、右侧主区域
- [x] 深色主题（`#0d1117` 背景）
- [x] 拖拽视觉反馈（蓝色虚线高亮）
- [x] 底部传输进度条

---

## 构建与运行

```bash
# 开发模式
npm install
npm run tauri dev

# 生产构建
npm run tauri build
# 产物：src-tauri/target/release/bundle/macos/WeTerm.app
#        src-tauri/target/release/bundle/dmg/WeTerm_0.1.0_aarch64.dmg
```

### 依赖

- macOS (Apple Silicon / Intel)
- Rust 工具链 (`rustup`, `cargo`)
- Node.js 18+
- 系统库：`libssh2` (通过 `ssh2` crate 自动编译)

---

## 开发历史与修复记录

| 问题 | 修复 |
|---|---|
| 端子双回显（"top" 显示为 "ttoopp"） | 移除 `t.write(key)` 本地回写，仅发送远端，由远端回显控制 |
| 终端未铺满全屏 | 添加 `setTimeout(fit.fit(), 50)` 延迟自适应 + ResizeObserver |
| 终端无滚动条 | 设置 `scrollback: 2000`，CSS 添加 `xterm-viewport` 自定义滚动条 |
| 链接断后历史不持久 | Rust struct 加 `#[serde(rename_all = "camelCase")]` 匹配前端 `authType` |
| 远程文件拖到桌面出现 JSON 文本 | 禁用远程文件的 `draggable`，本地文件保留拖拽 |

---

## 已知限制

1. **从 Finder 拖文件到 App 上传** — Web `dataTransfer.files` 在 Tauri 中不暴露完整文件路径，暂不支持
2. **Tabs 顺序不可拖拽调整**
3. **无 SCP/SFTP 目录递归上传/下载** — 当前仅支持单文件传输
4. **无 SSH 隧道 / 端口转发**
5. **无多窗口支持**

---

## 主要命令清单（Tauri Commands）

| 命令 | 说明 |
|---|---|
| `ssh_connect` | SSH 连接（密码 / Key / Agent） |
| `ssh_disconnect` | 断开连接并清理 |
| `ssh_terminal_write` | 写入 shell 数据 + 返回即时输出 |
| `ssh_terminal_read` | 读 shell 缓冲区（轮询用） |
| `ssh_execute` | 执行单条命令（非 shell 模式） |
| `sftp_list_files` | 列出远程目录 |
| `sftp_download` | 下载文件（推送进度事件） |
| `sftp_upload` | 上传文件（推送进度事件） |
| `sftp_read_file` | 读取远程文件内容（文本） |
| `sftp_write_file` | 写远程文件内容（文本） |
| `sftp_delete` | 删除远程文件或目录 |
| `sftp_create_dir` | 创建远程目录 |
| `sftp_rename` | 重命名远程文件或目录 |
| `local_list_files` | 列出本地目录 |
| `get_home_dir` | 获取用户 HOME 路径 |
| `save_connections` | 保存连接历史 |
| `load_connections` | 加载连接历史 |

---

## 数据持久化

- 连接历史路径：`~/.weterm/connections.json`
- 格式：JSON 数组，最多保留 20 条
- 去重规则：相同 `host + username` 自动去重，保留最新

---

## 配色方案（终端）

基于 GitHub Dark 配色：

```
背景    #0d1117
前景    #c9d1d9
光标    #c9d1d9
黑色    #484f58  亮黑    #6e7681
红色    #ff7b72  亮红    #ffa198
绿色    #3fb950  亮绿    #56d364
黄色    #d29922  亮黄    #e3b341
蓝色    #58a6ff  亮蓝    #79c0ff
品红    #bc8cff  亮品红  #d2a8ff
青色    #39c5cf  亮青    #56d4dd
白色    #b1bac4  亮白    #f0f6fc
```

---

## 未来计划

- [ ] Finder → App 拖拽上传（使用 Tauri DragDrop 事件获取文件路径）
- [ ] 目录递归上传/下载
- [ ] 标签页拖拽排序
- [ ] SSH 隧道/端口转发
- [ ] 会话保存/恢复
- [ ] 多窗口
- [ ] 国际化
